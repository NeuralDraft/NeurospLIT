// Dummy file to give the build system a second compilation unit and avoid duplicate stringsdata collisions.
// Intentionally minimal.

import Foundation

@inline(__always) func _whipTipNoop() { /* no-op */ }
